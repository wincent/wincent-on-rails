***************
*** 296,301 ****
  
  
    private
      def read_fixture_files
        if File.file?(yaml_file_path)
          # YAML fixtures
--- 296,302 ----
  
  
    private
+ 
      def read_fixture_files
        if File.file?(yaml_file_path)
          # YAML fixtures
***************
*** 332,338 ****
          end
        elsif File.file?(csv_file_path)
          # CSV fixtures
-         reader = CSV::Reader.create(erb_render(IO.read(csv_file_path)))
          header = reader.shift
          i = 0
          reader.each do |row|
--- 333,341 ----
          end
        elsif File.file?(csv_file_path)
          # CSV fixtures
+         separator = ',' 
+        	line_ending = determine_line_ending(csv_file_path) 
+        	reader = CSV::Reader.create(erb_render(IO.read(csv_file_path)), separator, line_ending)
          header = reader.shift
          i = 0
          reader.each do |row|
