%h1
  Tags:
  =(@tags[:found].collect {|t| t.name}).join ', '
-@taggings.each do |group, taggings|
  %h3=pluralizing_count taggings.length, group.downcase
  %table
    %tr
      %th Item
      %th When
    -for tagging in taggings
      %tr
        %td=taggable_link tagging.taggable
        %td=timeinfo tagging.taggable
