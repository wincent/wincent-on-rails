%h1 Translations
-for @translation in @translations
  %li
    -# this might need to be in a partial?
    -# TODO: pagination
    .label Locale code
    .attribute=h @translation.locale.code
    .lable Description
    .attribute=h @translation.locale.description
    .lable Key
    .attribute=h @translation.key
    .lable Translation
    .attribute=in_place_editor_field :translation, :translation, {}, :nested => :locale
    -# TODO: link_to editor
