%h2
  =result_number
  \:
  =link_to model.title, article_path(model)
.when=timeinfo model
%span.annotation Wiki article
.excerpt=truncate(model.body, :length => 240, :safe => true).w :base_heading_level => 2
