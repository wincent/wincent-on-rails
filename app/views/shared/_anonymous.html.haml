.notice
  You are posting <em>anonymously</em> so your submission will be held for moderation.
  =link_to 'Log in', login_path(:original_uri => request.fullpath)
  or
  -# BUG: passing the original_uri param here does nothing (not propagated)
  =link_to 'sign up', new_user_path(:original_uri => request.fullpath)
  to avoid this delay.
