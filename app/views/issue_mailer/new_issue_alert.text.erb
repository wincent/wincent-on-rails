A new issue (<%= @issue.awaiting_moderation? ? '' : 'not '%>awaiting moderation) has been posted.

Edit/moderate this issue:
  <%= @issue_url %>

Edit/moderate all issues:
  <%= @moderation_url %>

Summary: <%= raw @issue.summary %>
<% unless @issue.description.blank? %>
---- Start issue description ----
<%= raw @issue.description %>
---- End issue description ----
<% end %>
