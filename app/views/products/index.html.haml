=breadcrumbs 'Products'
-page_title 'Products'
-# TODO: add floating navbar on left, make center column narrower, when scrolling keep navbar visible (short product listing to jump up and down page); ie. use fixed positioning
-@products.each do |category, products|
  -unless category.blank?
    %h2.major="#{category} products"
  -products.each do |product|
    %div[product]
      %h3=link_to product.name, product_path(product)
      =raw product.description
