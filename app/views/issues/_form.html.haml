%table.main
  %tr
    %th=f.label :kind, 'Kind'
    %td=f.select :kind, underscores_to_spaces(Issue::KIND).sort
  %tr
    %th
      =f.label :summary, 'Summary'
      %br
      %span.annotation a one-line summary of your issue
    %td=f.text_field :summary
  %tr
    %th
      =f.label :product, 'Product'
      %br
      %span.annotation optional (leave blank for general issues)
    %td=f.select :product_id, product_options, :include_blank => true
  %tr
    %th
      =f.label :description, 'Description'
      %br
      %span.annotation
        please be as detailed as possible
        %br
        ==(#{wikitext_cheatsheet})
    %td=f.text_area(:description)
  %tr
    %th
      =f.label :public, 'Public?'
      -if !logged_in?
        %br
        %span.annotation
          this ticket must be public because you are posting anonymously
          ==(#{link_to 'log in', login_path(:original_uri => request.request_uri)})
    %td
      =f.check_box :public, { :disabled => !logged_in? }
      allow other visitors to see this ticket? (it may be helpful to them)
  -if admin?
    %tr
      %th=f.label :tags, 'Tags'
      %td=f.text_field :pending_tags
