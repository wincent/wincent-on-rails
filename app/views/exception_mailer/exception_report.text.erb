OVERVIEW:
---------

          Exception: <%= @exception.class %>
            Message: <%= raw @exception.message %>
  Controller#action: <%= @controller_name %>#<%= @controller_action %>
                URL: <%= raw @request.url %>
          Remote IP: <%= raw @request.remote_ip %>
                PID: <%= raw $$ %>
               Host: <%= raw `hostname`.chomp %>
               Time: <%= Time.now %> (report generation time)

BACKTRACE:
----------

  RAILS_ROOT  = <%= raw Rails.root %>
  BUNDLE_PATH = <%= raw Bundler.bundle_path %>

<%= raw @backtrace %>

SESSION:
--------

    Session ID: <%= raw @request.session['session_id'].to_s %>
    Parameters: <%= raw @request.filtered_parameters.inspect %>
  Session data: <%= raw @request.session.inspect %>


ENVIRONMENT:
------------

<%= raw @env %>
