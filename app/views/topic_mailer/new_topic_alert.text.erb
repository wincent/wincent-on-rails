A new topic (<%= @topic.awaiting_moderation? ? '' : 'not '%>awaiting moderation) has been posted.

Edit/moderate: <%= @edit_topic_url %>
<% unless @topic.awaiting_moderation? %>
Show: <%= @topic_url %>
<% end %>
Moderation dashboard: <%= @moderation_url %>

Title: <%= raw @topic.title %>

---- Start topic body ----
<%= raw @topic.body %>
---- End topic body ----
