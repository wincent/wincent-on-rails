A new topic (<%= @topic.awaiting_moderation? ? '' : 'not '%>awaiting moderation) has been posted.

Edit/moderate this topic:
  <%= @topic_url %>

Edit/moderate all topics:
  <%= @moderation_url %>

Title: <%= raw @topic.title %>

---- Start topic body ----
<%= raw @topic.body %>
---- End topic body ----
