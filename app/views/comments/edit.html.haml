%h1
  Edit comment on
  =link_to_commentable @comment.commentable
%div[@comment]
  =render 'form'
  .links
    =link_to 'show', comment_path(@comment)
    =button_to_destroy_model @comment
    -button_to_moderate_comment_as_ham @comment if @comment.awaiting_moderation?
.links
  =link_to 'all comments awaiting moderation', comments_path
  =link_to 'administator dashboard', admin_dashboard_path
